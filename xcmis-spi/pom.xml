<project>
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.xcmis</groupId>
		<artifactId>xcmis-parent</artifactId>
		<version>1.0-SNAPSHOT</version>
	</parent>

	<artifactId>xcmis-spi</artifactId>
	<name>xCMIS :: SPI</name>

	<dependencies>
		<dependency>
			<groupId>javax.xml.bind</groupId>
			<artifactId>jaxb-api</artifactId>
		</dependency>
		<dependency>
			<groupId>com.sun.xml.bind</groupId>
			<artifactId>jaxb-impl</artifactId>
		</dependency>
		<dependency>
			<groupId>org.exoplatform.core</groupId>
			<artifactId>exo.core.component.security.core</artifactId>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.jvnet.jaxb2.maven2</groupId>
				<artifactId>maven-jaxb2-plugin</artifactId>
				<version>0.6.2</version>
				<executions>
					<execution>
						<id>generate cmis classes</id>
						<goals>
							<goal>generate</goal>
						</goals>
						<phase>generate-sources</phase>
						<configuration>
							<schemaDirectory>src/main/resources</schemaDirectory>
							<catalog>src/main/resources/catalog.xml</catalog>
							<schemaLanguage>XMLSCHEMA</schemaLanguage>
							<extension>true</extension>
							<schemaIncludes>
								<include>CMIS-Core.xsd</include>
								<include>CMIS-Messaging.xsd</include>
								<include>CMIS-RestAtom.xsd</include>
							</schemaIncludes>
							<!--                     <verbose>true</verbose>-->
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>